---
- name: Configure
  ansible.builtin.include_role:
    name: prometheus.prometheus._common
    tasks_from: configure.yml
  vars:
    _common_system_user: "{{ meshtastic_exporter_system_user }}"
    _common_system_group: "{{ meshtastic_exporter_system_group }}"
    _common_config_dir: "{{ meshtastic_exporter_config_dir }}"
    _common_tls_server_config: "{{ meshtastic_exporter_tls_server_config }}"
    _common_http_server_config: "{{ meshtastic_exporter_http_server_config }}"
    _common_basic_auth_users: "{{ meshtastic_exporter_basic_auth_users }}"
  tags:
    - meshtastic_exporter
    - configure
    - meshtastic_exporter_configure

- name: Create textfile collector dir
  ansible.builtin.file:
    path: "{{ meshtastic_exporter_textfile_dir }}"
    state: directory
    owner: "{{ meshtastic_exporter_system_user }}"
    group: "{{ meshtastic_exporter_system_group }}"
    mode: u+rwX,g+rwX,o=rX
  become: true
  when: meshtastic_exporter_textfile_dir | length > 0
  tags:
    - meshtastic_exporter
    - configure
    - meshtastic_exporter_configure
